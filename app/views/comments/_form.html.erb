<div class="col-md-6 col-md-offset-3">
<%= bootstrap_form_for([@image, @image.comments.build]) do |f| %>
  <p>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
</div>
